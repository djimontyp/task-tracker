---
title: "Unified Onboarding System - UX Flow Research"
created: 2026-01-04
tags:
  - дослідження
  - frontend
  - onboarding
  - ux
status: draft
---

# Unified Onboarding System - UX Flow Research

## Мета дослідження

Переосмислити систему онбордингу користувача для досягнення наступних цілей:
- Об'єднати 3 різні онбординг компоненти в єдину систему
- Створити елегантне, ненав'язливе UX рішення
- Дозволити користувачам легко пропускати, повертатись та відновлювати процес налаштування
- Інтегрувати онбординг в CommandCenter як частину "mission control" естетики

## Контекст

### Що спонукало дослідження

Під час огляду візуальної естетики карток візарду на Dashboard (сітка 2x2) виявилось, що:
1. Картки виглядають "важкими" через замки та високу непрозорість
2. Концепція сітки 2x2 загалом перевантажує інтерфейс
3. Існує надлишковість: 3 різні UI компоненти для онбордингу

### Проблема

Користувач має 3 різні touchpoint для setup процесу, що створює плутанину:
- **OnboardingWizard** - модальний діалог з 5 кроками
- **MinimalOnboardingWizard** - вертикальний чеклист для Dashboard
- **SetupWizard** - сітка 2x2 з картками кроків

## Знахідки

### Поточний стан

| Компонент | Використання | Проблеми |
|-----------|--------------|----------|
| `OnboardingWizard` | Модальний діалог при першому візиті | Занадто детальний, важко пропустити |
| `MinimalOnboardingWizard` | Вертикальний список на Dashboard | Не використовується в production |
| `SetupWizard` | Сітка 2x2 карток кроків | Візуально важкий, "замки" обтяжують UI |

**Кроки онбордингу:**
1. Підключити Telegram
2. Створити проєкт
3. Увімкнути AI агента
4. Імпортувати історію (опціонально)

### Проблеми

1. ❌ **Візуальна перевантаженість**: Сітка 2x2 займає багато місця, locked стани з великими замками виглядають громіздко
2. ❌ **Надлишковість компонентів**: 3 різні UI для однієї функції
3. ❌ **Відсутність гнучкості**: Незрозуміло як повернутись до налаштування після skip
4. ❌ **Порушення дизайн-системи**: SetupWizard не узгоджується з high-tech естетикою CommandCenter

### Можливості

✅ **Compact Banner Integration** - інтегрувати онбординг прямо в CommandCenter як частину HUD  
✅ **Progressive Disclosure** - показувати тільки поточний крок, не весь workflow одразу  
✅ **Flexible Return Path** - додати Settings/Setup tab для повернення до налаштування  
✅ **State Persistence** - зберігати прогрес в localStorage для seamless experience  

## Альтернативні концепції

Розглянуто 4 альтернативи:

### 1. Linear Stepper
Горизонтальний прогрес-бар з кроками, під ним розгорнута картка поточного кроку.
- ✅ Компактніше (~200px)
- ✅ Звична UX практика
- ❌ На мобілці може бути тісно

### 2. Compact Banner (обрано)
Slim горизонтальний banner (~80-100px) інтегрований в CommandCenter area.
- ✅ Мінімальне місце
- ✅ Вписується в "mission control" естетику
- ✅ Не відволікає, але завжди видно
- ✅ Можна згортати/розгортати

### 3. Minimal Checklist
Вертикальний чеклист з expandable кроками.
- ✅ Простий і зрозумілий
- ✅ Схожий на TODO list
- ❌ Менш "вау-ефектний"

### 4. Single Active Card
Показується тільки поточний крок, інші повністю приховані.
- ✅ Максимально сфокусований
- ❌ Незрозуміло скільки кроків залишилось

## Обране рішення: 3-Level System

### Level 1: Setup Banner (завжди видимий)
Компактний banner інтегрований в CommandCenter.

**States:**
- **Active Setup** - розгорнутий з CTA кнопками
- **In Progress** - показує прогрес + наступний крок
- **Collapsed** - згорнутий до іконки з прогресом (при skip)
- **Completed** - "Все готово! Аналізуємо дані..."

### Level 2: Quick Actions (опціонально)
Action callbacks прямо з banner:
- Connect Telegram → redirect to Settings
- Create Project → open ProjectForm modal
- Enable Agent → redirect to Agents

### Level 3: Detailed Guide Modal (на запит)
OnboardingWizard залишається для тих хто потребує детальних інструкцій.
- Trigger: кнопка "Need Help?" в banner
- Або автоматично при першому візиті

## User Flow

### Сценарій 1: Перший візит (Cold Start)
```
Dashboard load
  ↓
Banner: "Почати роботу (0/4)" + "Підключіть Telegram"
  ↓
[Connect Telegram] → redirect to Settings
  ↓
After connection → Banner auto-updates: "1/4 ✓ Створіть проєкт"
```

### Сценарій 2: Skip & Return
```
User clicks "Skip"
  ↓
Banner згортається: [🚀 1/4]
  ↓
Hover: "Continue setup (3 steps remaining)"
  ↓
Click → banner розгортається знову
```

### Сценарій 3: Повернення через Settings
```
Settings → "Setup" tab
  ↓
Повний чеклист всіх кроків
  ├─ Completed: ✓
  ├─ Skipped: "Resume"
  └─ Locked: сірий
```

## Файли

| Файл | Зміни |
|------|-------|
| `SetupWizard.tsx` | ❌ Видалити (замінити на SetupBanner) |
| `MinimalOnboardingWizard.tsx` | ❌ Видалити (не використовується) |
| `OnboardingWizard.tsx` | ✅ Зберегти (для детальних інструкцій) |
| `CommandCenter.tsx` | 🔧 Інтегрувати SetupBanner |
| `DashboardPresenter.tsx` | 🔧 Замінити SetupWizard на SetupBanner |
| **NEW** `SetupBanner.tsx` | ✨ Створити компактний banner компонент |
| **NEW** `Settings/SetupTab.tsx` | ✨ Додати tab для повернення до налаштування |

## State Management

```typescript
interface SetupState {
  step1_telegram: 'pending' | 'completed' | 'skipped'
  step2_project: 'locked' | 'pending' | 'completed' | 'skipped'  
  step3_agent: 'locked' | 'pending' | 'completed' | 'skipped'
  step4_import: 'locked' | 'pending' | 'completed' | 'skipped'
  bannerState: 'expanded' | 'collapsed' | 'dismissed'
  showDetailedGuide: boolean
}
```

**Persistence:** `localStorage` з auto-restore через 7 днів якщо не завершено

## Результати імплементації

### ✅ SetupBanner - Successfully Implemented

**Створено:** `/features/onboarding/components/SetupBanner.tsx`

**Характеристики:**
- Компактний banner (~100px expanded, ~60px collapsed)
- Круговий прогрес-індикатор в collapsed state
- Інтегрується з Telegram store для real-time статусу
- 3 стани: expanded, collapsed, completed
- Smooth transitions та hover effects

**Тестування на localhost:80:**
- ✅ Expandable/collapsible functionality works
- ✅ Circular progress визуально ідеальний
- ✅ Кнопки "Connect Telegram", "Need Help?" працюють
- ✅ Auto-updates при зміні статусу

**Порівняння:**

| Метрика | SetupWizard (2x2) | SetupBanner (NEW) |
|---------|-------------------|-------------------|
| Висота | ~500px | ~100px / ~60px |
| Візуальна вага | Важка | Легка |
| Стан locked | Великі замки | Прихований |
| Прогрес | Незрозумілий | Circular indicator |
| UX | Статична сітка | Collapsible, dynamic |

---

## Критичний аналіз: OnboardingWizard Step 4

### Виявлені проблеми

**Мануальне тестування показало технічну катастрофу на Кроці 4 (Import):**

#### 1. **i18n Деградація (Critical)**
```
Замість перекладів:
- import.title
- import.description  
- onboarding.import.depth.skip
- import.error.fetchFailed
```

**Impact:** Користувач бачить "нутрощі коду" замість людської мови

#### 2. **API Failures (Blocking)**
```
422 Unprocessable Entity - /api/estimates
404 Not Found - невідомі endpoints
```

**Impact:** Система не може оцінити об'єм даних для імпорту

#### 3. **UI Bugs (Blocking)**
- Кнопка "Продовжити" outside of viewport
- Modal overflow проблеми
- Елементи перекривають інтерактивні зони

#### 4. **Логічна проблема**
Wizard просить імпортувати дані коли:
- Telegram може бути не підключений
- Немає перевірки з'єднання
- Незрозуміло чи є що імпортувати

### Рішення: Educational-First Approach

**Ключовий інсайт від користувача:**
> "Маємо ознайомити користувачів, навіть якщо не будуть налаштовувати зараз - хай знають що є такий функціонал і навіщо"

**Новий підхід:**

#### SetupBanner → Quick Setup (Action-First)
```
Фокус: швидко розпочати роботу
Кроки: 1-3 (Telegram, Project, Agent)
Імпорт: опціональний, не блокує
```

#### OnboardingWizard → Educational Guide (Learn-First)
```
Фокус: пояснити можливості системи
Показати: що може Pulse Radar
Дати вибір: налаштувати зараз або пізніше
```

**Plan:**
1. ✅ **SetupBanner** працює - швидкий setup без блокувань
2. ⏳ **Fix OnboardingWizard Step 4:**
   - Додати всі i18n ключі
   - Зробити Import truly optional (skip-friendly)
   - Пофіксити API calls або fallback gracefully
   - Виправити UI overflow issues
3. ⏳ **Позиціонування OnboardingWizard:**
   - Не auto-show при першому візиті
   - Trigger через "?" в header або Settings
   - Анонсувати features, не форсувати setup

---

## Висновки

### Що засвоєно

1. **Dual-Track Onboarding:**
   - **Quick Setup** (SetupBanner) - для action-oriented users
   - **Educational Guide** (OnboardingWizard) - для exploration-oriented users

2. **Не форсувати все одразу:**
   - Критичні кроки (1-3) через banner
   - Optional features (Import) через wizard або Settings

3. **Educational Value ≠ Blocking Flow:**
   - Показати можливості
   - Не вимагати налаштування прямо зараз
   - "Learn now, setup later" approach

4. **Technical Debt Real:**
   - OnboardingWizard Step 4 непрацездатний
   - SetupBanner обходить ці проблеми
   - Wizard треба фіксити, але він не блокує users

### Наступні кроки

1. ✅ SetupBanner implemented and tested
2. ⏳ Fix OnboardingWizard Step 4 critical bugs
3. ⏳ Розглянути header placement для global visibility
4. ⏳ Створити Settings/Setup tab для returning users
5. ⏳ Винести Import в окрему секцію Settings
6. ⏳ Додати inline help text на сторінках замість modal

## Пов'язане

- [[onboarding_ux_flow.md]] - Детальний UX flow з ASCII diagrams
- [[Wizard_Design_Review.md]] - Попередній аналіз wizard карток
- [[MASTER_UX_Product_Analysis.md]] - Загальний UX аналіз проєкту

## Візуальні референси

Створено mockup'и для альтернативних концепцій:
- `wizard_concept_linear.png` - Linear stepper
- `wizard_concept_banner.png` - Compact banner (обрано)
- `wizard_concept_checklist.png` - Minimal checklist
