# Pulse Radar Frontend — Handoff 2025-12-27

## Проект

**Pulse Radar** — AI-система збору знань з Telegram. Core flow: `Messages → AI extraction → Atoms → Topics → Dashboard`

**Stack:** React 18, TypeScript 5.9, shadcn/ui, TanStack Query, Zustand, Vite

---

## Сесія 2025-12-27 — Результати

### Виконано (3 агенти паралельно)

| Задача | Статус | Артефакти |
|--------|--------|-----------|
| Hero subtitle динамічний | ✅ | `useHeroSubtitle()` hook в DashboardPage |
| i18n keys (uk/en) | ✅ | `locales/*/dashboard.json` — subtitle.* |
| HumanizedLoader | ✅ | `shared/components/HumanizedLoader/` + 10 stories |
| Smart Filters UX spec | ✅ | `.obsidian-docs/знання/ux/smart-filters-spec.md` |
| Storybook Router fix | ✅ | MemoryRouter decorator в stories |
| Плани детально розширено | ✅ | Progress tables, quick wins, implementation notes |

### Прогрес

```
Pulse Radar Renovation:  19/33 (58%) ████████████░░░░░░░░
Frontend Transformation: 20/21 (95%) ███████████████████░
```

---

## Наступні задачі (пріоритет)

### 1. Smart Filters реалізація 🔥
**Spec готовий:** `.obsidian-docs/знання/ux/smart-filters-spec.md`

```
[All (124)]  [Signals (47)]  [Noise (77)]
```

**Файли створити:**
- `pages/MessagesPage/SmartFilters.tsx`
- `pages/MessagesPage/useFilterParams.ts`

**URL params:** `?filter=signals`, `?filter=noise`

### 2. Skeleton замість Loader
**Проблема:** HumanizedLoader з spinner для data loading — краще skeleton

**Де замінити:**
- DashboardMetrics → MetricCardSkeleton
- RecentInsights → InsightCardSkeleton
- TopTopics → TopicListSkeleton
- MessagesPage → MessageCardSkeleton

### 3. Today's Focus блок
**Dashboard:** Card з top-3 PENDING_REVIEW atoms

### 4. Tech Cleanup (low priority)
- Видалити `backend/app/models/legacy.py` (Task, Source)
- Оновити seed scripts

---

## Інструменти

### Агенти (ПАРАЛЕЛЬНО для швидкості)

```python
# Запуск паралельно в одному message
Task(subagent_type="Frontend Expert (F1)", prompt="...", run_in_background=True)
Task(subagent_type="UX Specialist (U1)", prompt="...", run_in_background=True)

# Перевірка статусу (non-blocking)
TaskOutput(task_id="abc123", block=False)

# Чекати результат
TaskOutput(task_id="abc123", block=True)
```

**Доступні агенти:**
- `Frontend Expert (F1)` — React, TypeScript, shadcn/ui
- `UX Specialist (U1)` — Accessibility, user flows, WCAG
- `Visual Designer (V1)` — Design tokens, colors
- `Super QA (Q1)` — Tests, Playwright
- `Backend Expert (B1)` — FastAPI, SQLModel

### MCP Playwright (браузер)

```python
mcp__playwright__browser_navigate(url="http://localhost:6006")
mcp__playwright__browser_snapshot()        # a11y tree
mcp__playwright__browser_take_screenshot() # PNG
mcp__playwright__browser_click(element="Button", ref="e123")
mcp__playwright__browser_wait_for(time=3)
```

### Skills

```python
Skill(skill="frontend")                    # React patterns
Skill(skill="frontend-design:frontend-design")  # UI design
Skill(skill="smart-commit")                # Git commits
Skill(skill="api-contracts")               # Sync API types
```

---

## Команди

```bash
# Services
just services-dev              # Docker watch
just storybook                 # http://localhost:6006

# Frontend
cd frontend
npm run storybook              # Storybook dev
npm run build                  # Build
npx tsc --noEmit               # TypeScript
npm run lint                   # ESLint
npm run generate:component     # Plop

# Перевірка прогресу
cat .obsidian-docs/плани/pulse-radar-renovation.md | grep -E "^\[.\]"
```

---

## Файлова структура

```
frontend/src/
├── pages/
│   ├── DashboardPage/
│   │   ├── index.tsx              # Container + useHeroSubtitle
│   │   ├── DashboardPresenter.tsx # Pure UI + subtitle prop
│   │   └── components/            # Metrics, Insights, Topics
│   └── MessagesPage/
│       ├── index.tsx              # ← Smart Filters тут
│       ├── MessageCard.tsx
│       └── MessagesSummaryHeader.tsx
├── shared/
│   ├── components/
│   │   ├── HumanizedLoader/       # NEW ✅
│   │   └── SearchBar/
│   ├── ui/                        # shadcn primitives
│   ├── tokens/                    # Design tokens
│   └── patterns/                  # CardWithStatus, EmptyState
└── public/locales/
    ├── uk/*.json                  # Ukrainian
    └── en/*.json                  # English

.obsidian-docs/
├── плани/
│   ├── pulse-radar-renovation.md  # UX план (головний)
│   └── frontend-transformation.md # Tech план
└── знання/
    └── ux/
        └── smart-filters-spec.md  # SPEC для реалізації
```

---

## Quick Start

```bash
# 1. Запустити Storybook
cd frontend && npm run storybook &

# 2. Відкрити spec
cat .obsidian-docs/знання/ux/smart-filters-spec.md

# 3. Реалізувати Smart Filters
# → pages/MessagesPage/SmartFilters.tsx
```

---

## Design System нагадування

```tsx
// ❌ ЗАБОРОНЕНО
<div className="bg-red-500 p-3 gap-5">

// ✅ ПРАВИЛЬНО
<div className="bg-semantic-error p-4 gap-4">

// Status badges — завжди icon + text
<Badge className={badges.status.connected}>
  <CheckCircle className="h-3.5 w-3.5" />
  Connected
</Badge>
```

**Touch targets:** min 44px (`h-11`)
**Spacing:** кратне 4px (gap-2, gap-4, p-4, p-6)

---

## Продовжити

**Пріоритет 1:** Smart Filters — spec готовий, потрібен код

```
Frontend Expert (F1) → SmartFilters.tsx + useFilterParams.ts
UX Specialist (U1) → Review accessibility
Super QA (Q1) → Tests
```
