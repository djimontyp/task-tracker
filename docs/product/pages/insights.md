# Insights

## Мета

PM бачить **ВСІ** витягнуті знання (задачі, ідеї, рішення) в одному місці з можливістю фільтрації.

## Цільова аудиторія

| Роль | Потреба |
|------|---------|
| PM | "Де всі задачі які обговорювали?" |
| Team Lead | "Які рішення прийняли цього тижня?" |

---

## Wireframe

```
┌─────────────────────────────────────────────────────────────────┐
│  INSIGHTS                                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ [🔍 Пошук...]                                               ││
│  │                                                              ││
│  │ Тип: [Всі ▼] [Задачі] [Ідеї] [Рішення] [Проблеми]          ││
│  │ Топік: [Всі топіки ▼]    Період: [Цей тиждень ▼]           ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  📊 Знайдено: 47 insights                                       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 🔴 TASK                                          14:32     ││
│  │ ─────────────────────────────────────────────────────────── ││
│  │ Виправити баг з авторизацією                                ││
│  │                                                              ││
│  │ Користувачі не можуть залогінитись через Google OAuth.      ││
│  │ Помилка 500 на callback endpoint.                           ││
│  │                                                              ││
│  │ 📍 Mobile App Development  👤 @petro  💬 3 messages         ││
│  │                                                              ││
│  │ [Переглянути контекст]  [Експорт →]                         ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 💡 IDEA                                          вчора      ││
│  │ ─────────────────────────────────────────────────────────── ││
│  │ Додати dark mode в налаштування                             ││
│  │                                                              ││
│  │ Багато користувачів просять темну тему, особливо для       ││
│  │ роботи ввечері.                                             ││
│  │                                                              ││
│  │ 📍 UI/UX  👤 @maria  💬 5 messages                          ││
│  │                                                              ││
│  │ [Переглянути контекст]  [Зберегти]  [Відхилити]             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ ✅ DECISION                                      2 дні      ││
│  │ ─────────────────────────────────────────────────────────── ││
│  │ Використовуємо PostgreSQL замість MongoDB                   ││
│  │                                                              ││
│  │ Прийнято рішення залишитись на PostgreSQL через pgvector   ││
│  │ та кращу підтримку транзакцій.                              ││
│  │                                                              ││
│  │ 📍 Backend API  👤 @ivan  💬 12 messages                    ││
│  │                                                              ││
│  │ [Переглянути контекст]                                      ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  [← Попередня]  Сторінка 1 з 5  [Наступна →]                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Типи Atoms (візуальне кодування)

| Тип | Іконка | Колір (semantic) | Label |
|-----|--------|------------------|-------|
| TASK | `CircleDot` | `destructive` | "Задача" |
| IDEA | `Lightbulb` | `warning` | "Ідея" |
| DECISION | `CheckCircle` | `success` | "Рішення" |
| PROBLEM | `AlertTriangle` | `destructive` | "Проблема" |
| QUESTION | `HelpCircle` | `info` | "Питання" |
| INSIGHT | `Gem` | `primary` | "Інсайт" |

---

## Компоненти

### 1. InsightFilters

```tsx
interface InsightFiltersProps {
  search: string;
  onSearchChange: (value: string) => void;

  types: AtomType[];
  onTypesChange: (types: AtomType[]) => void;

  topicId: string | null;
  onTopicChange: (id: string | null) => void;

  period: 'today' | 'week' | 'month' | 'all';
  onPeriodChange: (period: string) => void;
}
```

**UI елементи:**
- Search input з debounce
- Type chips (multi-select toggle)
- Topic dropdown з пошуком
- Period dropdown

---

### 2. InsightCard

```tsx
interface InsightCardProps {
  id: string;
  type: AtomType;
  title: string;
  content: string;
  topicName: string;
  topicIcon?: string;
  authorName: string;
  messageCount: number;
  createdAt: string;
  status?: 'pending' | 'approved' | 'rejected';
  onViewContext: () => void;
  onExport?: () => void;
  onApprove?: () => void;
  onReject?: () => void;
}
```

**Структура:**

```
┌─────────────────────────────────────────────────────────────────┐
│ [Type Icon] TYPE_LABEL                          [Time Badge]    │
│ ─────────────────────────────────────────────────────────────── │
│ Title (font-semibold)                                           │
│                                                                 │
│ Content (text-muted-foreground, max 3 lines, truncate)         │
│                                                                 │
│ 📍 Topic  👤 Author  💬 N messages                             │
│                                                                 │
│ [Actions based on status]                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3. ContextModal

Показує оригінальні повідомлення, з яких витягнуто insight.

```tsx
interface ContextModalProps {
  atomId: string;
  open: boolean;
  onClose: () => void;
}
```

**Контент:**
- Atom деталі (title, content, type)
- Список пов'язаних повідомлень:
  - Author avatar
  - Content
  - Timestamp
  - Highlight matched text

**API:**
```
GET /api/v1/atoms/{id}/messages
Response: {
  atom: { ... },
  messages: [
    { id, content, authorName, sentAt }
  ]
}
```

---

## Фільтри

### Type Filter (multi-select chips)

```tsx
<div className="flex flex-wrap gap-2">
  {ATOM_TYPES.map(type => (
    <Button
      key={type}
      variant={selected.includes(type) ? 'default' : 'outline'}
      size="sm"
      onClick={() => toggle(type)}
    >
      <TypeIcon type={type} className="mr-1.5 h-4 w-4" />
      {TYPE_LABELS[type]}
    </Button>
  ))}
</div>
```

### Topic Filter (dropdown з пошуком)

```tsx
<Select value={topicId} onValueChange={setTopicId}>
  <SelectTrigger>
    <SelectValue placeholder="Всі топіки" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="all">Всі топіки</SelectItem>
    {topics.map(t => (
      <SelectItem key={t.id} value={t.id}>
        {t.icon} {t.name}
      </SelectItem>
    ))}
  </SelectContent>
</Select>
```

### Period Filter

```tsx
const PERIODS = [
  { value: 'today', label: 'Сьогодні' },
  { value: 'week', label: 'Цей тиждень' },
  { value: 'month', label: 'Цей місяць' },
  { value: 'all', label: 'Весь час' },
];
```

---

## API

### List Atoms

```
GET /api/v1/atoms
  ?page=1
  &per_page=20
  &search=авторизація
  &types=TASK,PROBLEM
  &topic_id=uuid
  &created_after=2025-12-01
  &sort=-created_at

Response: {
  items: Atom[],
  total: 47,
  page: 1,
  pages: 3
}
```

### Atom with Messages

```
GET /api/v1/atoms/{id}?include=messages

Response: {
  id, type, title, content, ...
  messages: [
    { id, content, authorName, sentAt }
  ]
}
```

---

## Стани

### Loading

```tsx
<div className="space-y-4">
  {[1,2,3].map(i => (
    <Card key={i}>
      <CardContent className="p-4">
        <Skeleton className="h-4 w-24 mb-2" />
        <Skeleton className="h-6 w-3/4 mb-4" />
        <Skeleton className="h-4 w-full mb-2" />
        <Skeleton className="h-4 w-2/3" />
      </CardContent>
    </Card>
  ))}
</div>
```

### Empty (no results)

```tsx
<EmptyState
  icon={SearchIcon}
  title="Нічого не знайдено"
  description="Спробуйте змінити фільтри або пошуковий запит"
/>
```

### Empty (no data)

```tsx
<EmptyState
  icon={SparklesIcon}
  title="Поки немає insights"
  description="AI витягне знання після аналізу повідомлень"
  action={<Button>Запустити аналіз</Button>}
/>
```

---

## Дії з Insight

| Дія | Коли показувати | Результат |
|-----|-----------------|-----------|
| Переглянути контекст | Завжди | Відкриває ContextModal |
| Експорт | Для approved | Copy to clipboard |
| Зберегти | Для pending | Approve atom |
| Відхилити | Для pending | Reject atom |

---

## Responsive

| Breakpoint | Layout |
|------------|--------|
| mobile | Filters collapsed, 1 column |
| tablet | Filters row, 1 column cards |
| desktop | Full layout |

---

## Файли

```
src/pages/InsightsPage/
├── index.tsx              # Layout + state management
├── components/
│   ├── InsightCard.tsx
│   ├── InsightFilters.tsx
│   └── ContextModal.tsx
└── hooks/
    └── useInsights.ts     # Query + filters state
```
