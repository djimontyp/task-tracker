/**
 * FTS (Full-Text Search) types for keyword search API.
 *
 * Re-exported from shared for backwards compatibility.
 * The canonical types are now in @/shared/components/SearchBar/types/fts.ts
 */

export type {
  FTSTopicBrief,
  FTSTopicResult,
  FTSMessageResult,
  FTSAtomType,
  FTSAtomResult,
  FTSSearchResultsResponse,
} from '@/shared/components/SearchBar/types/fts'
