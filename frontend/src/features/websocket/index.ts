// Both hooks moved to @/shared/hooks - re-export for backwards compatibility
export { useWebSocket, useServiceStatus } from '@/shared/hooks'
export type { WebSocketState, UseWebSocketOptions, IndicatorStatus, HealthState } from '@/shared/hooks'
