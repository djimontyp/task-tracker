/**
 * {{pascalCase name}} Feature Types
 * {{description}}
 */

/**
 * {{pascalCase name}} entity
 */
export interface {{pascalCase name}} {
  id: string;
  // Add entity fields here
  createdAt: string;
  updatedAt: string;
}

/**
 * Input for creating a new {{pascalCase name}}
 */
export interface Create{{pascalCase name}}Input {
  // Add creation fields here
}

/**
 * Input for updating an existing {{pascalCase name}}
 */
export interface Update{{pascalCase name}}Input {
  // Add update fields here (all optional for partial updates)
}

/**
 * {{pascalCase name}} list filters
 */
export interface {{pascalCase name}}Filters {
  search?: string;
  // Add filter fields here
}
