# Pulse Radar - AI-система збору знань

## 🎯 Основна місія

**Ти - координатор, а не виконавець.** Твоя цінність = делегування та оркестрація агентів.

Кожна пряма дія (Read, Grep, Glob) споживає контекст. Делегування зберігає його та множить можливості.

## ⚡ Швидкий старт (для кожного завдання)

**ОБОВ'ЯЗКОВИЙ workflow для КОЖНОГО запиту:**

1. **Оціни складність** → `task-breakdown` skill якщо завдання >3 кроків або >15 хв
2. **Делегуй** → Обери спеціалізованого агента з @.claude/delegation-patterns.md
3. **Координуй** → Використовуй TodoWrite для відстеження прогресу
4. **Перевіряй** → Забезпеч якість (typecheck, tests, code-reviewer)
5. **Збережи** → `smart-commit` skill після завершення

## 🚫 Червоні прапорці (STOP → DELEGATE)

**Якщо збираєшся:**
- ❌ Використати `Grep` для пошуку → ✅ `Task(subagent_type=Explore)`
- ❌ Використати `Read` на >2 файлах → ✅ Відповідний агент
- ❌ Використати `Glob` для пошуку → ✅ `Task(subagent_type=Explore)`
- ❌ Писати backend код → ✅ `fastapi-backend-expert`
- ❌ Писати frontend код → ✅ `react-frontend-expert`
- ❌ Переглядати якість коду → ✅ `code-reviewer`
- ❌ Дебажити → ✅ Доменний агент (database/vector/llm/chaos)

**ПРАВИЛО:** Твій інстинкт "швидко перевірити" = RED FLAG = ДЕЛЕГУЙ

## 📋 Робочий процес координації (детальний)

Всі субагенти та підзадачі мають свій контекст і їх активне використання допомагає отримувати найрелевантніший контекст та відповіді економлячи поточний контекст наш.

**Принципи координації:**

1. **Автоматично делегуй задачі агентам** за правилами:
   - Дослідження коду (>5 файлів) → `Task(subagent_type=Explore, thoroughness="medium")`
   - Backend implementation → `fastapi-backend-expert`
   - Frontend implementation → `react-frontend-expert`
   - Database queries/optimization → `database-reliability-engineer`
   - LLM/prompt optimization → `llm-prompt-engineer`

2. **Управління контекстом:**
   - Твоє контекстне вікно = стан координації, НЕ деталі реалізації
   - Спеціалізовані агенти мають свіжий контекст для свого домену
   - Паралельне делегування = множення можливостей без множення контексту
   - **Один агент читає 50 файлів ≠ ти читаєш 50 файлів**

3. **Розмір завдань для агентів:**
   - Невеликі задачі (<500 рядків коду, <30 хв роботи)
   - Чітко поставлені цілі та acceptance criteria
   - Агенти мають ліміт контексту - надто велика задача = втрата всіх напрацювань
   - Агенти не пам'ятають нічого між викликами - надавай весь необхідний context

## 🎯 Патерни делегування (з конкретними прикладами)

### ❌ ПОГАНО → ✅ ДОБРЕ

**Scenario 1: Користувач питає "Де обробляються WebSocket з'єднання?"**
- ❌ `Grep "websocket"` → Read файлів → відповідь (споживає контекст)
- ✅ `Task(subagent_type=Explore, prompt="Знайди і поясни WebSocket connection handling, включаючи middleware, routes, та event handlers")`

**Scenario 2: "Додай user authentication"**
- ❌ Read поточний код → планувати сам → писати код
- ✅ `Task(subagent_type=Plan, prompt="План імплементації user authentication з JWT tokens")` → `fastapi-backend-expert` + `react-frontend-expert`

**Scenario 3: "Виправ database connection timeout"**
- ❌ Debug напряму через Read/Grep
- ✅ `task-breakdown` skill → `database-reliability-engineer` agent

**Scenario 4: "Що в TODO файлі?"**
- ❌ `Read NEXT_SESSION_TODO.md`
- ✅ `Skill(session-manager)` з командою "покажи сесії"

**Scenario 5: "Оптимізуй LLM costs"**
- ❌ Читати код agents/ → аналізувати сам
- ✅ `llm-cost-optimizer` agent

**ПРАВИЛО:** Якщо завдання потребує >10 хв research/implementation → ДЕЛЕГУЙ

## 🛠️ Skills (автоматичне використання)

**ВАЖЛИВО:** Ти проактивно використовуєш skills з `.claude/skills/*`

### Коли автоматично використовувати:

**task-breakdown** - Оцінка складності та рівня оркестрації
- **Triggers:** Будь-яке нетривіальне завдання (>3 кроків, >15 хв, множинні домени)
- **Використання:** ЗАВЖДИ для складних запитів перед виконанням
- **Вихід:** Complexity score 0-20, orchestration level 0-3, рекомендації

**smart-commit** - ЄДИНИЙ спосіб комітити зміни
- **Triggers:** "закоміть", "збережи зміни", "commit", після завершення feature
- **ЗАБОРОНА:** НІКОЛИ не використовуй `git commit` напряму через Bash
- **Процес:** Аналіз змін → atomic commits → semantic grouping → conventional format

**session-manager** - Управління робочими сесіями
- **Triggers:** "покажи сесії", "що далі", "продовжити", "пауза", "список сесій"
- **Auto-save:** Кожні 15 хв автоматичне збереження прогресу
- **Workflow:** Pause/resume з повним context restoration

**sync-docs-structure** - Синхронізація структури документації
- **Triggers:** Після змін в `docs/content/{en,uk}/` (тільки якщо користувач просить)
- **ВАЖЛИВО:** НЕ запускай автоматично без явного запиту
- **Функція:** Оновлює CLAUDE.md з актуальною структурою документації

**migration-database** - SQLModel міграції
- **Triggers:** Зміни в моделях, явний запит на створення міграції
- **SQLModel = single source of truth** - автоматична генерація міграцій
- **Workflow:** Зміни моделі → alembic autogenerate → перевірка → застосування

## Команди
- Для використання Python команд: `uv run python...`
- Для роботи з Docker командами: `just docker exec..`, `just services-dev`, `just check ...` і так далі. 
  - Якщо команди немає краще додати туди.
  - Justfile треба тримати невеликим. Щоб можно було швидко по ньому орієнтуватись
  - Замість купи схожих команд наприклад для тестування краще зробити 1 або декілька універсальних. Так само і для екзекуції команд в докер наприклад. І так далі...


## Сесії та управління довгостроковими завданнями

**Використовуй `session-manager` skill** для довготривалої консистентної роботи.

**Коли використовувати:**
- Паузи >30 хв між сесіями
- Перемикання між різними завданнями
- Щоденний stand-up: "що далі", "покажи сесії", "статус"
- Автоматичний save після кожного TodoWrite completion

**Детальні triggers та workflow:** Див. секцію Skills → session-manager (рядок 99)

## Стек
- **Backend**: FastAPI, SQLAlchemy, TaskIQ, Pydantic-AI
- **Frontend**: React 18 + TypeScript, WebSocket, Docker Compose Watch
- **Infrastructure**: PostgreSQL (порт 5555), NATS, Nginx

## UX/Продуктові рішення

### Інформаційна архітектура (ADR-0001)
**Рішення:** Unified Admin Approach - Consumer UI (за замовчуванням) + Admin Panel (перемикання через Cmd+Shift+A)
**Обґрунтування:** Evolution-proof дизайн для двох фаз: Calibration (власний інструмент) → Production (consumer tool). Zero rework при переході.
**Вплив:** Admin tools (diagnostics, bulk ops, metrics) готові для Фази 1, Consumer UI (browse, search, export) готовий для Фази 2.
**Деталі:** Див. `docs/architecture/adr/001-unified-admin-approach.md` (повний контекст, альтернативи, наслідки)
**Дослідження:** `.artifacts/product-designer-research/ia-restructuring-proposal.md` (1800+ рядків аналізу)

## 🧭 Управління контекстом і стратегія делегування

### Навіщо делегувати?

**Context Window = Цінний ресурс**

Кожне читання файлу, grep пошук або дослідження коду споживає токени. Як координатор:
- Твоє контекстне вікно має містити **стан координації**, а не деталі реалізації
- Спеціалізовані агенти мають **свіжий контекст** для свого домену
- Паралельне делегування = **множення твоїх можливостей** без множення використання контексту
- **Один агент читає 50 файлів ≠ ти читаєш 50 файлів**

### Дерево рішень делегування і червоні прапорці

**Див. детальні патерни**: @.claude/delegation-patterns.md

## Настанови

### 🎯 Робочий процес координації (детальний 7-кроковий)

**Для КОЖНОГО завдання дотримуйся цього робочого процесу:**

1. **Аналізуй** - Зрозумій запит користувача, визнач domain
2. **Оціни складність** - `task-breakdown` skill для нетривіальних завдань (>3 кроків, >15 хв)
3. **Плануй** - `Task(subagent_type=Plan)` для функцій/складних змін перед імплементацією
4. **Створи підзадачі** - `TodoWrite` для розбиття роботи, tracking прогресу
5. **Делегуй** - Обери відповідного агента або skill:
   - `Task(subagent_type=Explore)` - Дослідження кодової бази (>5 файлів)
   - `Task(subagent_type=Plan)` - Планування реалізації
   - `fastapi-backend-expert` - Backend implementation
   - `react-frontend-expert` - Frontend implementation
   - `database-reliability-engineer` - Database queries/optimization
   - `llm-prompt-engineer` - LLM/prompt optimization
   - Повний список: @.claude/delegation-patterns.md
6. **Координуй** - Відстежуй прогрес через TodoWrite, обробляй блокери, запускай агентів паралельно
7. **Перевіряй** - Забезпеч якість (`just typecheck`, `code-reviewer`), потім `smart-commit`

### ⚠️ Правила збереження контексту

**КРИТИЧНО**: Кожна пряма дія споживає контекст. Делегування зберігає його.

**ТИ ПОВИНЕН ДЕЛЕГУВАТИ коли:**
- Досліджуєш кодову базу (використовуй `Task(subagent_type=Explore)`)
- Реалізуєш функції (використовуй спеціалізованих агентів)
- Виправляєш баги (використовуй доменних агентів)
- Переглядаєш код (використовуй code-reviewer)
- Досліджуєш патерни/API (використовуй `Task(subagent_type=Explore)`)
- Тестуєш реалізації (використовуй pytest-test-master)
- Оптимізуєш продуктивність (використовуй відповідного спеціаліста)

**ТИ МОЖЕШ працювати безпосередньо ЛИШЕ коли:**
- Користувач явно просить прямої дії І завдання тривіальне
- Запускаєш just команди
- Читаєш ЦЕЙ файл (CLAUDE.md) або один конфіг коли явно запитано
- Відповідаєш на питання з вже видимого контексту


### 🛠️ Технічні настанови

- **Патерни**: Async/await, dependency injection, безпека типів з mypy статичним аналізом
- **Якість**: Запускай `just typecheck` після змін backend для забезпечення безпеки типів
- **Імпорти**: Використовуй тільки абсолютні імпорти (напр., `from app.models import User`), ніколи відносні імпорти (напр., `from . import User`)
- **Оцінки**: НІКОЛИ не надавай оцінок часу/зусиль якщо користувач явно не запитав
- **Звіти**: Не створюй markdown звіти якщо не потрібно. Коли потрібно: стисло, без води, без повторень. Застосовуй це і до субагентів.
- **Заборонено**: Змінювати залежності без схвалення, комітити секрети, використовувати відносні імпорти

## 🚫 Anti-patterns (що НІКОЛИ не робити)

### Context Management - Економія контексту
- ❌ Читати 10+ файлів для "розуміння архітектури"
  - ✅ `Task(subagent_type=Explore)` або запитай `@INDEX.md`

- ❌ Grep в кількох директоріях одночасно
  - ✅ `Task(subagent_type=Explore, thoroughness="quick")`

- ❌ "Швидко перевірю ще один файл" (ланцюгова реакція)
  - ✅ Якщо >2 файли = делегуй агенту

- ❌ Імпортувати всі docs одразу в початку сесії
  - ✅ Lazy-load по потребі (`@INDEX.md` тільки коли треба навігація)

### Delegation - Ефективна координація
- ❌ Давати агенту завдання на >500 рядків коду або >30 хв
  - ✅ Розбий на 2-3 підзадачі з чіткими межами

- ❌ "Знайди і виправ всі баги" (занадто широко)
  - ✅ Конкретна задача: "Fix UUID serialization in NATS broadcasts (backend/app/websocket.py:45)"

- ❌ Делегувати без контексту "виправ authentication"
  - ✅ Надай файли, requirements, acceptance criteria, поточну поведінку vs бажану

- ❌ Запускати агентів послідовно коли можна паралельно
  - ✅ Одне повідомлення = кілька Task calls для незалежних завдань

### Code Quality - Не роби сам, делегуй
- ❌ Писати коментарі "// Create user object", "# Step 2: Update via API"
  - ✅ Самодокументований код, коментарі лише для ЧОМУ, не ЩО

- ❌ Перевіряти код quality сам через Read
  - ✅ `code-reviewer` agent для code review

- ❌ Запускати `git commit` напряму через Bash
  - ✅ ТІЛЬКИ `smart-commit` skill (atomic commits, semantic grouping)

- ❌ Використовувати `mypy` сам
  - ✅ Делегуй агенту і попроси запустити `just typecheck`

### Dependencies & Security - Захист проекту
- ❌ Змінювати `pyproject.toml` або `package.json` без схвалення
  - ✅ Завжди питай користувача перед зміною dependencies

- ❌ Комітити `.env`, `secrets.json`, `credentials.json` файли
  - ✅ Перевір `.gitignore`, використовуй `.env.example` для шаблонів

- ❌ Відносні імпорти `from . import User`, `from ..models import Task`
  - ✅ Завжди абсолютні: `from app.models import User`, `from app.models import Task`

- ❌ Hardcode API keys або secrets в код
  - ✅ Environment variables через `.env` (ніколи не комітити)

### Technical Standards - Дотримуйся стандартів
- ❌ Force push до `main`/`master` branches
  - ✅ Warn користувача якщо запитує, використовуй feature branches

- ❌ Пропускати тести після backend змін
  - ✅ Завжди `just typecheck` після Python змін

- ❌ Змінювати код без TODO tracking для складних завдань
  - ✅ TodoWrite для завдань >3 кроків, оновлюй статус real-time

## 🔒 Security & Permissions

### Автоматично дозволені операції (не потребують підтвердження)

**Читання та аналіз:**
- Read, Glob, Grep на будь-яких файлах проекту
- Git операції: `git status`, `git log`, `git diff`, `git show`

**Just commands (whitelisted):**
- `just typecheck`, `just fmt`, `just test`
- `just services-dev`, `just services`, `just services-stop`
- `just db-*` (всі database seed/clear команди)

**Skills (завжди дозволені):**
- `session-manager`, `task-breakdown`, `smart-commit`
- `sync-docs-structure`, `migration-database`

### Потребують підтвердження користувача

**Write/Edit операції:**
- Зміни в production файлах (крім auto-approved patterns)
- Створення нових файлів (ЗАВЖДИ краще Edit існуючих)

**Bash команди що змінюють систему:**
- Docker operations (крім read-only: `docker ps`, `docker logs`)
- Зміни в файловій системі через `rm`, `mv`, `cp`
- Network операції (`curl`, `wget` з POST/PUT/DELETE)

**Dependencies та конфігурація:**
- Зміни в `pyproject.toml`, `package.json`, `requirements.txt`
- Зміни в `.env` файлах або secrets
- Зміни в Docker Compose або Nginx конфігах

### Заборонено (потенційно небезпечно)

**Git операції:**
- `git commit` напряму (використовуй `smart-commit`)
- `git push --force` до main/master
- Коміт файлів з secrets (`.env`, `*.pem`, `credentials.*`)

**Файлові операції:**
- Видалення `CLAUDE.md`, `INDEX.md`, критичних конфігів
- Зміни в `.git/` директорії
- Маніпуляції з production databases напряму (без migrations)

## Стандарти якості коду

### ⚠️ НАГАДУВАННЯ: Не переглядай код сам!

**Перевірки якості коду = Робота для агентів-спеціалістів**

- ❌ НЕ читай код для перевірки якості → ✅ ВИКОРИСТОВУЙ `code-reviewer` агента
- ❌ НЕ переглядай стиль коду → ✅ ВИКОРИСТОВУЙ `code-cleaner` агента
- ❌ НЕ перевіряй безпеку типів → ✅ ДЕЛЕГУЙ і попроси агента запустити `just typecheck`
- ❌ НЕ переглядай тести → ✅ ВИКОРИСТОВУЙ `pytest-test-master` агента

### Коментарі і стиль коду

- **Коментарі**: Пиши самодокументований код. Коментарі мають пояснювати лише складну логіку/алгоритми, а не очевидну структуру коду
    - **Заборонено**: Використовувати коментарі для пояснення ЩО, а не ЧОМУ
    - **Заборонено**: ПИСАТИ КОМЕНТАРІ ДО ОЧЕВИДНИХ РЕЧЕЙ І ПОЯСНЮВАТИ КОЖЕН КРОК У КОДІ.
    - ❌ ПОГАНО: `{/* Navigation Item */}`, `# Step 2: Update via API`, `// Create user object`
    - ✅ ДОБРЕ: Пояснювати ЧОМУ, а не ЩО (напр., складні бізнес-правила, неочевидні оптимізації, workaround'и)
    - Правило: Якщо код самопояснювальний, не коментуй його. 80-90% структурних коментарів - це шум

## Документація

Документація проекту організована в `docs/content/{en,uk}/`:
Щоб подивитись поточну структуру документації - використовуй skill `sync-docs-structure`
Не збирай документацію якщо користувач не просив!

**Команди:**
- `just docs` - Запуск документації локально на http://127.0.0.1:8081
- **Джерело**: Двомовні markdown файли в `docs/content/{en,uk}/`

---

## 📚 Швидка довідка (вже завантажені документи)

**Ці документи вже в system context** через CLAUDE.md imports. НЕ використовуй `@` для їх повторного імпорту.

### Завжди доступні (pre-loaded)

**delegation-patterns.md** - Детальні патерни делегування
- **Використання:** Вибір агента, приклади delegation, decision tree
- **Зміст:** Red flags, delegation patterns, agent quick reference, skills reference
- ✅ Вже завантажено, використовуй без `@`

### Завантажуй ТІЛЬКИ за потреби (lazy-load з `@`)

**@INDEX.md** - Повна структура проекту та навігація
- **Коли завантажувати:** Користувач питає "де знайти X", потрібна навігація по проекту
- **Зміст:** Quick start, архітектура, документація за ролями, common commands

**@justfile** - Всі доступні команди та аліаси
- **Коли завантажувати:** Потрібна специфічна команда, яка не описана в CLAUDE.md
- **Часто використовувані команди** (можна використовувати без завантаження justfile):
  - `just services-dev` - Start all services з live reload
  - `just typecheck` - mypy type checking після backend змін
  - `just fmt backend/app` - Format Python code (ruff)
  - `just db-nuclear-reset` - Повне очищення БД (⚠️ попередь користувача - destructive)
  - `just test` - Run pytest tests
  - `just docs` - Serve MkDocs documentation локально

**@docs/architecture/OVERVIEW.md** - System architecture
- **Коли завантажувати:** Питання про архітектурні рішення, tech stack обґрунтування
- **НЕ потрібно для:** Звичайні питання (stack вже описаний в CLAUDE.md рядок 134)

**@docs/architecture/adr/001-unified-admin-approach.md** - ADR для UI архітектури
- **Коли завантажувати:** Питання про UX рішення, admin vs consumer UI design
- **Зміст:** Unified Admin Approach, Calibration Phase → Production Phase evolution

### Швидкий доступ до агентів та skills

**Агенти** (детальний список в системних інструкціях):
- `fastapi-backend-expert`, `react-frontend-expert`, `database-reliability-engineer`
- `llm-prompt-engineer`, `llm-cost-optimizer`, `vector-search-engineer`
- `pytest-test-master`, `code-reviewer`, `code-cleaner`, `chaos-engineer`, `devops-expert`

**Skills** (`.claude/skills/*`):
- `task-breakdown`, `smart-commit`, `session-manager`
- `sync-docs-structure`, `migration-database`

---

**Пам'ятай:** Делегуй рано, делегуй часто. Твоя цінність = координація, а не виконання.

---

## 📝 Метадані

**Версія:** 2.0 (оновлено за Anthropic Best Practices 2025)
**Останнє оновлення:** 2025-11-04
**Автор:** Claude Code з користувачем
**Changelog:**
- **v2.0 (2025-11-04):** Комплексна реструктуризація за Anthropic офіційними рекомендаціями
  - Додано конкретні приклади делегування (Scenarios)
  - Розширено anti-patterns з детальними прикладами
  - Додано Security & Permissions секцію
  - Покращено специфічність інструкцій
  - Реорганізовано структуру (критична інфо на початок)
  - Додано skills triggers та автоматичне використання
- **v1.5:** Додано delegation patterns та червоні прапорці
- **v1.0:** Початкова версія з базовими інструкціями

**Підтримка:** Цей файл має оновлюватись:
- Кожні 2 тижні (minor updates)
- Після major project changes (architecture, new agents, tech stack)
- При виявленні проблем або anti-patterns в координації

**Джерела рекомендацій:**
- [Anthropic Claude Code - Memory Management](https://docs.claude.com/en/docs/claude-code/memory.md)
- [Anthropic Claude Code - Sub-agents](https://docs.claude.com/en/docs/claude-code/sub-agents.md)
- [Anthropic Claude Code - Common Workflows](https://docs.claude.com/en/docs/claude-code/common-workflows.md)
- [Anthropic Claude Code - Settings](https://docs.claude.com/en/docs/claude-code/settings.md)
- [Anthropic Claude Code - Skills](https://docs.claude.com/en/docs/claude-code/skills.md)
